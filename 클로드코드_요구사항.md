# Content Aggregator - 클로드코드 구현 요구사항

## 프로젝트 개요

**목적**: 고객에게 아침 시황(미국) 매일 자동 전송

**현재 상황**: 수동으로 시황 복사 → Gemini 붙여넣기 → 요약 → 텔레그램 전송 (귀찮음)

**목표**: 완전 자동화 (GitHub Actions 또는 로컬 PC)

---

## 핵심 기능 (MVP)

### 1. 시황 데이터 수집
- **Briefing.com** 크롤링 (BeautifulSoup4)
  - 마감 후 30분 이내 업데이트되는 시황 수집
  - 당일 마감시황만 수집
- **텔레그램 채널** 크롤링 (requests)
  - 공개 텔레그램 채널에서 최신 시황 메시지 수집
  - 웹 스크래핑 방식 (https://t.me/s/{channel_username})

### 2. 요약 생성
- **Gemini API**로 요약
  - 수집된 시황 데이터를 Gemini API에 전달
  - 한국어로 간결하게 요약
  - 주요 지수, 환율, 금리, 원자재 가격 포함

### 3. 텔레그램 전송
- **python-telegram-bot** 사용
  - 요약된 메시지를 텔레그램 봇으로 전송
  - 메시지 길이 제한 처리 (4096자)
  - 전송 실패 시 에러 처리

### 4. 자동 실행
- **실행 시간**: 미국 마감 후
  - 일반: 한국시간 6시 (UTC 21:00)
  - 서머타임: 한국시간 5시 (UTC 20:00)
- **실행 환경**: GitHub Actions (우선), 로컬 PC (대안)

---

## 기술 스택

- **언어**: Python 3.11+
- **크롤링**: 
  - BeautifulSoup4 (Briefing.com)
  - requests (텔레그램 채널)
- **요약**: Gemini API (google-generativeai)
- **전송**: python-telegram-bot (v20+)
- **스케줄링**: GitHub Actions (cron)

---

## 파일 구조

```
content-aggregator/
├── collectors/
│   ├── __init__.py
│   ├── briefing_collector.py      # Briefing.com 크롤링
│   └── telegram_channel_collector.py  # 텔레그램 채널 크롤링 (웹 스크래핑)
├── processors/
│   ├── __init__.py
│   └── gemini_summarizer.py       # Gemini API 요약
├── senders/
│   ├── __init__.py
│   └── telegram_sender.py        # 텔레그램 전송 (기존 코드 활용 가능)
├── config.py.example              # 설정 예제
├── main.py                        # 메인 스크립트
├── requirements.txt               # 패키지 목록
├── .github/workflows/
│   └── daily.yml                  # GitHub Actions 워크플로우
└── README.md                      # 사용 가이드
```

---

## 구현 상세 사항

### 1. Briefing.com 크롤링 (`collectors/briefing_collector.py`)

**요구사항**:
- 마감 후 30분 이내 업데이트되는 시황 페이지 크롤링
- 당일 마감시황만 수집
- HTML 파싱하여 텍스트 추출

**참고**: morning-market-summary의 collectors.py에 유사한 패턴 있음

**구현 예시**:
```python
class BriefingCollector:
    def get_market_wrap(self) -> Optional[str]:
        """Briefing.com에서 마감시황 가져오기"""
        # 마감시황 페이지 URL 확인 필요
        # BeautifulSoup으로 파싱
        # 텍스트 추출
```

### 2. 텔레그램 채널 크롤링 (`collectors/telegram_channel_collector.py`)

**요구사항**:
- 공개 텔레그램 채널 웹 버전 크롤링
- 최신 메시지 수집
- 당일 메시지만 필터링

**참고**: morning-market-summary의 TelegramChannelCollector 참고

**구현 예시**:
```python
class TelegramChannelCollector:
    def __init__(self, channel_username: str):
        self.channel_username = channel_username
        self.base_url = f"https://t.me/s/{channel_username}"
    
    def get_latest_message(self) -> Optional[str]:
        """텔레그램 채널의 최신 메시지 가져오기"""
        # requests로 웹 페이지 가져오기
        # BeautifulSoup으로 파싱
        # 최신 메시지 텍스트 추출
```

### 3. Gemini 요약 (`processors/gemini_summarizer.py`)

**요구사항**:
- Gemini API 호출
- 수집된 시황 데이터를 프롬프트로 변환
- 한국어로 간결하게 요약
- 주요 지수, 환율, 금리, 원자재 가격 포함

**참고**: morning-market-summary의 gemini_summarizer.py 참고

**프롬프트 예시**:
```
다음은 오늘 미국 마감 시황 데이터입니다. 
한국어로 간결하고 이해하기 쉽게 요약해주세요.
주요 지수, 환율, 금리, 원자재 가격을 포함하세요.

[수집된 데이터]
```

### 4. 텔레그램 전송 (`senders/telegram_sender.py`)

**요구사항**:
- python-telegram-bot v20+ 사용
- 메시지 길이 제한 처리 (4096자)
- 전송 실패 시 에러 처리 및 로깅

**참고**: 
- 기존 content-aggregator의 telegram_sender.py 활용 가능
- morning-market-summary의 sender.py 참고

### 5. 메인 스크립트 (`main.py`)

**워크플로우**:
1. 시황 데이터 수집 (Briefing.com + 텔레그램)
2. 데이터 통합
3. Gemini API로 요약 생성
4. 텔레그램 전송
5. 에러 처리 및 로깅

**에러 처리**:
- 수집 실패 시 에러 로그
- 요약 실패 시 에러 로그
- 전송 실패 시 워크플로우 실패 (sys.exit(1))

### 6. GitHub Actions 워크플로우 (`.github/workflows/daily.yml`)

**요구사항**:
- 매일 실행 (미국 마감 후)
  - 일반: UTC 21:00 (한국시간 6시)
  - 서머타임: UTC 20:00 (한국시간 5시)
- Secrets에서 설정 읽기:
  - `GEMINI_API_KEY`
  - `TELEGRAM_BOT_TOKEN`
  - `TELEGRAM_SEND_TO_CHAT_ID`
  - `TELEGRAM_CHANNEL_USERNAME` (선택)
- config.py 자동 생성
- 설정 검증
- 실행 및 에러 처리

**참고**: morning-market-summary의 daily.yml 참고

---

## 설정 파일 (`config.py.example`)

```python
# Gemini API 설정 (필수)
GEMINI_API_KEY = "YOUR_GEMINI_API_KEY"

# 텔레그램 봇 설정 (필수)
TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN"
TELEGRAM_SEND_TO_CHAT_ID = None  # 숫자

# 텔레그램 채널 수집 (선택)
TELEGRAM_CHANNEL_USERNAME = ""  # @ 없이 채널명만

# Briefing.com 설정 (선택)
BRIEFING_MARKET_WRAP_URL = ""  # 마감시황 페이지 URL
```

---

## requirements.txt

```
beautifulsoup4>=4.12.0
requests>=2.31.0
google-generativeai>=0.3.0
python-telegram-bot>=20.0
python-dotenv>=1.0.0
```

---

## 참고 프로젝트

### morning-market-summary
- **URL**: https://github.com/hwangincheolpb/morning-market-summary
- **유사점**: 
  - 텔레그램 채널 크롤링 (TelegramChannelCollector)
  - Gemini API 요약 (GeminiSummarizer)
  - 텔레그램 전송 (sender.py)
  - GitHub Actions 워크플로우
- **차이점**:
  - Briefing.com 크롤링 추가 필요
  - 실행 시간 다름 (미국 마감 후)

### 기존 content-aggregator
- **위치**: `C:\dev\active-projects\content-aggregator\`
- **활용 가능**:
  - `senders/telegram_sender.py` (수정하여 사용)
  - 구조 참고

---

## 구현 우선순위

1. **Briefing.com 크롤링** (새로 구현)
2. **텔레그램 채널 크롤링** (morning-market-summary 참고)
3. **Gemini 요약** (morning-market-summary 참고)
4. **텔레그램 전송** (기존 코드 활용)
5. **메인 스크립트 통합**
6. **GitHub Actions 워크플로우**

---

## 테스트 방법

### 로컬 테스트
```bash
# 설정 파일 생성
cp config.py.example config.py
# config.py 수정 (API 키 입력)

# 실행
python main.py
```

### GitHub Actions 테스트
1. Secrets 설정
2. workflow_dispatch로 수동 실행
3. 로그 확인

---

## 주의사항

1. **Briefing.com 크롤링**:
   - 사이트 구조 변경 시 수정 필요할 수 있음
   - User-Agent 설정 필요
   - Rate limiting 고려

2. **텔레그램 채널 크롤링**:
   - 공개 채널만 가능
   - 웹 버전 구조 변경 시 수정 필요할 수 있음

3. **에러 처리**:
   - 전송 실패 시 워크플로우도 실패하도록 (sys.exit(1))
   - 상세한 에러 로깅

4. **서머타임 처리**:
   - 실행 시간이 변경되므로 수동 조정 필요
   - 또는 자동 감지 로직 추가

---

## 완료 기준

- [ ] Briefing.com에서 시황 수집 성공
- [ ] 텔레그램 채널에서 시황 수집 성공
- [ ] Gemini API로 요약 생성 성공
- [ ] 텔레그램 전송 성공
- [ ] GitHub Actions에서 자동 실행 성공
- [ ] 에러 처리 정상 작동
